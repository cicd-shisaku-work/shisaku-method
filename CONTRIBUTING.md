# CONTRIBUTING — git運用規約

**Version:** v0.5.2
**Date:** 2026/09/23
**性格：** 本リポジトリ（shisaku-method）のgit運用ルール。過去のgitログ（2026/06〜07）から実際に安定した運用を成文化したもの。本リポジトリはドキュメント中心であり、規約もそれに最適化する。

---

## 1. ブランチ運用（PRフロー）

```
main ← トピックブランチ（PR経由・squash merge）
```

- **mainへの直コミットはしない。** すべての変更はトピックブランチを切り、GitHub上のPull Requestを経由してmainへマージする。
- PRはソロ運用でもセルフレビューの場として使う——diffを俯瞰し、一晩置いて読み直してからマージしてよい。**確定は人間の目を通す**（SEFA・PEESの「評価器は提案し、確定は人間」と同じ思想）。
- マージ方式は **squash merge** とし、マージ後にトピックブランチを削除する。これにより「1トピック＝main上の1コミット」が保たれる。

### ブランチ命名

| プレフィックス | 用途 | 例 |
| :-- | :-- | :-- |
| `add-*` | 新規ドキュメント・概念の追加 | `add-trust-signal-frame-and-publications` |
| `update-*` | 既存ドキュメントの版上げ・構造改訂 | `update-pees-v0.2` |
| `fix-*` | 誤記・リンク・ライセンス表記などの修正 | `fix-license-metadata` |

- 名前は英小文字とハイフン。内容が推測できる具体名にする（`update-docs` のような汎用名は避ける）。
- 1ブランチ=1トピック。無関係な変更を同じブランチに混ぜない。

## 2. コミットメッセージ規約

Conventional Commits の簡易形を用いる。本リポジトリはドキュメント中心のため、大半は `docs:` である。

```
<type>: <summary in English, imperative mood, ~50 chars>
```

**言語：コミットメッセージ（要約・body）は英語で書く。** 本リポジトリは英語圏の読者・利用者を射程に含むため、mainのgit logは英語で読める状態を保つ。日本語での補足が必要な場合はPR説明に書く（PR説明は日英併記可）。

| type | 用途 |
| :-- | :-- |
| `docs:` | ドキュメントの追加・改訂（本リポジトリの既定） |
| `fix:` | 誤記・リンク切れ・メタデータ修正 |
| `chore:` | ディレクトリ構成・.gitignore 等の整備 |

### 書式ルール

- **版上げは遷移を明記する：** `docs: kosei-mining-definition v0.7.2 -> v0.7.3`
- **新規追加は add + 対象 + 版：** `docs: add SEFA evaluator fidelity axioms v0.1`
- 複数ファイルにまたがる1トピックは、要約に主対象を書く。

### 本文（body）の義務（コミットの自己完結性）

コミットメッセージは、**diffを開かなくても「何が・なぜ変わったか」が分かる**ことを合格条件とする。mainのgit logだけで各ドキュメントの改訂履歴が再構成できる状態を保つ。

**bodyが必須となる条件（いずれか該当）：**

1. ドキュメントの版上げ（マイナー・パッチとも）
2. 採点ロジック・条項・構造に触れる変更
3. 複数トピック・複数ドキュメントにまたがる変更
4. ファイルの追加・移動・削除・配置換え

bodyには次を書く（英語）：**変更点の箇条書き**（ドキュメントごと・条項単位）、**改訂の根拠**（検証結果・観測・上流文書・issue）、移動があれば**旧配置→新配置**。

**要約（1行目）だけでよいのは**、単一ファイルの誤記・リンク・表記修正のみ（例：`fix: broken canonical URL in trust-signal-frame`）。

**禁止例（過去ログの反省点）：**

- `修正` ／ `update` ——対象も内容も分からない
- `docs: add SEFA v0.1; pees v0.1.8 -> v0.2; add CONTRIBUTING`（bodyなし）——3トピックの構造改訂が1行に潰れ、何がどう変わったか辿れない

**参照は役割で書く（自己完結性の外延）：** メッセージは、**本リポジトリだけを読む者に意味が通る**こと。ここに無い文書・リポジトリ・作業単位の名／パス／節番号に依存しない——根拠を挙げるなら**役割**で書く（外部批評／別セッションの監査／ある文章媒体の実装／系列の統治文書）。ここに無いものの名は、読む側から辿れないため根拠として働かず、辿れない参照を履歴に残す。

**制作過程は書かない：** メッセージの対象は「何が・なぜ変わったか」であって、誰が・何を使って書いたかではない。

**セルフチェック（コミット前に二問）：** (1) 半年後の自分がこのメッセージだけを読んで、diffの中身を言い当てられるか。言い当てられないなら、bodyに書き足す。(2) 本リポジトリの外の名を消しても、メッセージの意味が通るか。通らないなら、根拠を役割と内容で書き直す。

**squash merge時の注意：** GitHubのsquashはローカルコミットのメッセージを連結して本文にする。bodyが確実に残るよう、`git commit -m "<要約>" -m "<body>"` の形でローカルコミット自体に本文を持たせる。PR説明にも同内容を書く（レビューの場で使うため）。

## 3. PR チェックリスト（収録と改訂）

**適用範囲は項目ごとに示す。**【新規】は新規ドキュメントを収録する PR に、**【新規・格上げ】はそれに加えて、既存文書を `publications/` から `concepts/` へ格上げする PR に**、【全PR】は改訂・修正を含むすべての PR に掛かる。

- [ ] 【新規】**ヘッダー**：タイトル／短縮名（あれば）／版のブロック（§4 の定型＝`**Version:** vX.Y.Z` と `**Date:**`）／上流依存（該当時）を冒頭に持つ。
- [ ] 【新規】**ライセンス表示**：末尾に `## License` セクション（CC BY 4.0、Copyright (c) 20XX shisaku、Author URL、Canonical repository URL）。
- [ ] 【全PR】**Canonical repository URLが配置先ディレクトリと一致**している（移動時は必ず書き換える）。
- [ ] 【新規】**README同期**：`concepts/` 配下への追加は、**主要な概念であれば**「含まれる概念」（日英）を更新する。README は索引であって網羅ではない——概念は増え続けるので、全件を載せると入口として働かなくなる。`publications/` 配下は README を触らない。
- [ ] 【全PR】**改訂の場合（境界＝ディレクトリ）**：`concepts/` 配下は **Version と Date だけ**を更新し（ビルドに載る文書はインデックスの `[meta]`、載っていない文書は本文ヘッダー＝§4「版の置き場」）、**本文に改訂履歴は置かない**——履歴は git log が正本（§2 の body 義務がそのためにある。本文に埋めると同じ履歴が二箇所で育って食い違い、版を重ねるほど本文が膨らむ）。`publications/` など `concepts/` 以外は、従来どおり本文の改訂履歴に版・日付・変更点・根拠を追記する（暫定運用・境界の是非は運用で見る）。
- [ ] 【全PR】**自立性**：本文・図・コミットメッセージが、**原典だけを読む者に意味が通る**（README「誰が、どこまで読むか」の一段目）。ここに無い文書・リポジトリ・作業単位の名／パス／節番号に依存していない（参照は役割で書く＝§2）。判別子＝その名を消して意味が通るか。漏れは新規追加より改訂で入る。
- [ ] 【全PR】**図**：図を持つ文書の版を上げたら、同じ PR で図を描き直し、図の版を本文の版に揃える。図は本文の監査の道具で、描き直しの中で見つかった穴は図で埋めず、本文の側で直すか記録する（`authoring-policy.md`「三層と、境界の判別子」の節）。
- [ ] 【全PR】**生成される文書**：`src/` にモジュールを持つ文書は、本文を直接編集しない。モジュールを編集し、`python3 src/engine/build.py` でビルドし、**ソースと生成物を同じコミットに入れる**。
- [ ] 【全PR】**ビルドの検査**：`python3 src/engine/build.py --check` が通る（生成物が最新であることの機械確認）。
- [ ] 【全PR】**影響半径の突き合わせ**：モジュールを変更した PR は、`python3 src/engine/build.py --impact` を通し、**参照している側と突き合わせた結果を PR 説明に書く**（どこを直したか／直す必要がなかったか）。モジュールは編集の単位であって読む単位ではないため、局所の正しさが全体の矛盾になる経路は、この突き合わせでしか塞げない。参照関係を持つモジュールが無い場合（新規収録など）は、その旨を 1 行書けば足りる。
- [ ] 【新規・格上げ】**配置基準**：**判別子は一問——その文書は、特定の成果物（note・Medium・Kindle の個別作品）から参照されるために在るか。** 在る → `publications/published/<媒体>/<言語>/<名>/`。どの成果物にもまだ紐づかない検証途上のもの → `publications/tmp/`。**理論の側に属し、道具世代に紐づいて変わるだけのもの**（ランタイム・運用手引・実装プロンプト）は `concepts/` に残す——**身分は置き場でなく様式が表す**（`authoring-policy.md`「種別」の節）。`publications/` から `concepts/` への格上げは `git mv` で行う（履歴が追える）。

**失敗機構**：「成果物に寄った」で線を引くと、成果物依存と道具依存が一語に潰れ、理論の実装層まで `publications/` へ送られる。送られた先は媒体別の棚なので、公開していない文書には入る枡が無く、置き場を探すために媒体を捏造することになる。

### 3.1 新しい文書を一本足すときの順

チェックリストは出来上がった PR を検査する。ここに置くのは、**その PR に載せるものを作る順**である。**構成 → 生成 → モジュール化**で、機械の操作は書かない——割り方と検査の手順は `src/engine/OPERATIONS.md`「文書を新しく載せる」の節が、割り方の規則は `src/engine/DESIGN.md`「モジュール」「インデックス」の節が持つ。ここが持つのは**段の順と、段の境目に何を置くか**だけである。

1. **構成——裁定は、書く前に降ろす。** 種別（原典・補足・解釈）が必須様式を選ぶので、`authoring-policy.md`「種別」の節で身分を決めてから枡を並べる。この段で降ろすのは、題・英題・上流依存・棚。**本文を書いてから題を決めると、題に合わせて本文が動き、同じ文書を二度書くことになる。**
2. **生成——一本の草稿として通しで書く。** モジュールに割ってから書かない。読む単位は文書であり、節ごとに書くと節の境目で語と呼吸が変わる。例は必ず入れる（価値が出るかは、例の上でしか試せない）。
3. **登録——新しい語は、本文より先に台帳へ。** `terminology-policy.md` 第6条。逆にすると、登録が本文からの書き写しになり、曖昧が検出されずに追認される。
4. **監査——置換の検査（解釈）または反証条件（原典）を当てる。** **この段までは、文書は一枚のファイルでよい。**
5. **モジュール化——割るのは、草稿が完成してからである。** 手順は `OPERATIONS.md` が持つ。この節が足すのは順序の一点だけ——**割る前に完成させ、割る回では内容を動かさない。**モジュール化は編集ではないので、生成物が草稿と一文字でも違えば割り方を間違えている。**以後の改訂は逆で、本文でなくモジュールを編集する**（上の「生成される文書」）。

**失敗機構**：モジュールから書き始めると、文書は節の寄せ集めになる——境目でしか露見せず、通しで読んだときに初めて分かる。逆に、割ったあとで内容を直すと、草稿と生成物のどちらが正本か決まらなくなる。**この順が守るのは、編集の単位（モジュール）と読む単位（文書）の違いである。**

## 4. バージョニング

- ドキュメントは版で管理する（`v0.1` → `v0.1.1` → `v0.2`）。
  - **パッチ（v0.1.x）**：条項の明確化・表記修正・採点ロジックに触れない変更。
  - **マイナー（v0.x）**：構造改訂・条項の追加削除・採点ロジックの変更。
- 版を上げたら、旧版ファイルを残さない。旧版はgit履歴が保存する（`v0.1` と `v0.2` のファイル並置はしない）。

### メジャー版は 0 のまま維持する

**版は `v0.x.y` を出ない。**

**① 身分。** 本リポジトリの文書は、対象の解釈である。

**② `v0` の理由。** 解釈は固定されない——記述した本人の IDION が、生きているかぎり更新され続けるからである。固定されないものに完成は無く、完成が無いものに完成版の表示を掲げれば、表示と実体が食い違う。

**③ `v1.0` の意味。** 本リポジトリにおいて `v1.0` は**完成版**——対象について検証可能な記述体系が成立し、記述がその体系として書き直された状態——を指す。SemVer の慣習（公開インターフェースが安定した最初のメジャー版）ではない。**この定義は本リポジトリ独自のものである。**

**④ 条件。** `v1.0` を名乗れるのは、**その対象について、第三者による観測・検証・再現が可能な記述体系が成立した場合に限る**。ヒトを対象とする文書であれば、脳の神経マップと機能が十分に明らかになり、認知過程が第三者に観測・検証・再現できるようになったときである。**条件が満たされないまま `v1.0` を掲げれば、科学の装いをまとった似非科学である。**

**⑤ 対象が工学化された場合。** その対象は、シサクメソッドの射程（工学化されていない対象を扱う）から出る。記述は別の系の仕事になる。工学化と検証可能性は別の軸であり、④の条件は工学化ではなく**検証可能性**に掛かる。**二つは同時に起きるとは限らない。** 対象が工学化されていなくても、第三者による観測・検証・再現が可能になれば、記述は `v1.0` を名乗れる。⑤が言うのは、そのあとに、あるいはそれとは独立に対象が工学化された場合のことである。**④が満たされたことが、⑤を引き起こすのではない。**

**⑥ 注意。** 更新が止まることは条件ではない。科学の記述もまた更新される。「更新しなくなったから完成した」は、完成の条件のすり替えである。

**失敗機構（④と⑤）。** 二つを同じ事と読むと、「検証できるようになった日に射程から出るのだから、`v1.0` には到達できない」となり、条件が達成不能の飾りになる。条件は、届かないものとして書かれていない。

### 版の置き場

- **ビルドに載っている文書（`src/` にモジュールを持つ文書）は、版と日付をインデックス（`src/docs/…/<文書>/index.toml` の `[meta]`）が持つ。** 本文には書かない。冒頭のヘッダーにも奥付にも、そこから差し込まれる。版上げは 1 か所の編集で済む。
- ビルドに載っていない文書は、本文ヘッダーが持つ。

### ヘッダーの定型

版のブロックは次の形に揃える。

```markdown
**Version:** v0.1.5
**Date:** 2026/09/07
```

- **ラベルは英語に統一する**（`Version:` / `Date:`）。日本語ラベル（`バージョン：`）は、文書を触ったときに英語へ寄せる。
- **版の後ろに括弧書きを置かない。** 但し書きが要るなら、本文の冒頭に一文として置く。版の行は、機械が読む行である。
- 奥付（末尾）を持つ文書は、そこでも同じ値を使う。
- 題・上流依存・Author などの行の有無は、文書の種類による（定型の対象はこの版のブロック）。

### ライセンス表示

- ライセンス表示は共通部品として持つ。ビルドに載っている文書は、共有モジュール `src/shared/license/cc-by-4.0.md` を呼び、文書ごとに変わる一文と配置先 URL だけを `[meta]` で渡す。
- **CC BY 4.0 以外を選ぶ余地を残す。** 別のライセンスを使う文書は、対応する共有モジュールを `src/shared/license/` に置き、そちらを呼ぶ。ライセンスは文書ごとの選択であり、共通部品はその既定を用意するだけである。

## 5. 履歴の衛生

- **push済みコミットのrebase・filter-branchはしない**（初期に履歴書き換えで荒れた反省による）。書き換えはローカルの未pushコミットに限る。
- squash mergeを使うため、トピックブランチ内のWIPコミットは自由でよい。mainに載る1コミットのメッセージだけ規約に従う。
- コミッターは `cicd <cicd@shisaku.work>` に統一する。

## 6. 標準フロー（コマンド例）

```bash
# 1. ブランチを切る
git checkout main && git pull origin main
git checkout -b update-pees-v0.2

# 2. 作業・コミット
git add <files>
git commit -m "docs: pees-prelim-business v0.1.8 -> v0.2"

# 3. push して PR を作る
git push -u origin update-pees-v0.2
gh pr create --fill   # または GitHub UI

# 4. セルフレビュー後、squash merge（GitHub UI推奨）
gh pr merge --squash --delete-branch
```

---

## License

This document defines the git workflow conventions for the shisaku-method repository.

Copyright (c) 2026 shisaku

Licensed under the Creative Commons Attribution 4.0 International (CC BY 4.0).

License: https://creativecommons.org/licenses/by/4.0/
Author: https://note.com/abstraction
Canonical repository: https://github.com/cicd-shisaku-work/shisaku-method
